import type { TaskPropertyTypes } from '@/types/template'

export const AllTaskPropertyTypes: TaskPropertyTypes[] = [
	'text',
	'number',
	'date',
	'boolean',
	'select',
	'multiSelect',
	'user'
]
