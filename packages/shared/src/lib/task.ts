import { TaskStandardUpdatableFieldLabels } from "@types";

export const StandardUpdatableFields = new Set<TaskStandardUpdatableFieldLabels>(["title", "content", "assignee", "status", "priority"]);
