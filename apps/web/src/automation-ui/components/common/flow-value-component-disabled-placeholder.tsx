import { Input } from "@/components/ui/input";

export const FlowValueComponentDisabledPlaceholer = () => {
  return <Input type="text" placeholder="Value" disabled={true} className="w-full h-8" />;
};
